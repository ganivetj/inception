version: '3'

services:
  wordpress:
    build:
      context: ./srcs/wordpress
    volumes:
      - wordpress_data:/var/www/html
    networks:
      - inception

  mariadb:
    build:
      context: ./srcs/mariadb
    ports:
      - "3306:3306"
    networks:
      - inception

  nginx:
    depends_on:
      - wordpress
    build: 
      context: ./srcs/nginx
    ports:
      - "80:80"
    volumes:
      - wordpress_data:/var/www/html
    networks:
      - inception

volumes:
  wordpress_data:

networks:
  inception:
    driver: bridge